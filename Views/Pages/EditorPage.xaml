<Page 
    x:Class="CourseProject.Views.Pages.EditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CourseProject.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:CourseProject.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DataPage"
    d:DataContext="{d:DesignInstance local:EditorPage, IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Row: Table Selection -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Current Table Label -->
            <ui:TextBlock 
                Grid.Column="0" 
                VerticalAlignment="Center"
                FontSize="16"
                FontWeight="SemiBold"
                Text="{Binding Path=ViewModel.CurrentTable}"/>

            <!-- Table Selection Buttons -->
            <StackPanel 
                Grid.Column="1" 
                Orientation="Horizontal">
                <ui:Button
                    Content="Мотоцикл"
                    Command="{Binding Path=ViewModel.LoadCommand}"
                    CommandParameter="Мотоцикл"
                    Appearance="Secondary"/>

                <ui:Button
                    Content="Фары"
                    Command="{Binding Path=ViewModel.LoadCommand}"
                    CommandParameter="Фары"
                    Appearance="Secondary"/>

                <ui:Button
                    Content="Шины"
                    Command="{Binding Path=ViewModel.LoadCommand}"
                    CommandParameter="Шины"
                    Appearance="Secondary"/>

                <ui:Button
                    Content="Компания"
                    Command="{Binding Path=ViewModel.LoadCommand}"
                    CommandParameter="Компания"
                    Appearance="Secondary"/>

                <ui:Button
                    Content="Человек"
                    Command="{Binding Path=ViewModel.LoadCommand}"
                    CommandParameter="Человек"
                    Appearance="Secondary"/>
            </StackPanel>
        </Grid>

        <!-- Second Row: Action Buttons -->
        <StackPanel 
            Grid.Row="1" 
            Orientation="Horizontal" 
            HorizontalAlignment="Right"
            Margin="0,0,0,15">
            <ui:Button
                Content="Добавить"
                Command="{Binding Path=ViewModel.AddCommand}"
                Icon="Add24"/>

            <ui:Button
                Content="Изменить"
                Command="{Binding Path=ViewModel.EditCommand}"
                Icon="Edit24"/>

            <ui:Button
                Content="Удалить"
                Command="{Binding Path=ViewModel.DeleteCommand}"
                Icon="Delete24"
                Appearance="Danger"/>

            <ui:Button
                Content="Перезагрузить"
                Command="{Binding Path=ViewModel.ReloadCommand}"
                Icon="ArrowClockwise24"/>
        </StackPanel>

        <!-- Data List -->
        <ui:ListView 
            Grid.Row="2" 
            x:Name="list">
            <ui:ListView.View>
                <ui:GridView x:Name="grid"/>
            </ui:ListView.View>
        </ui:ListView>
    </Grid>
</Page>